<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.classLink.model.mapper.ClassStudentMapper">

    <!-- 학생 수강반 등록 -->
    <insert id="insertClassStudent">
        INSERT INTO CLASS_STUDENT (
            STUDENT_NO,
            CLASS_NO,
            MEMBER_NO
        )
        VALUES (
                   SEQ_CSNO.NEXTVAL,
                   #{classNo},
                   #{memberNo}
               )
    </insert>

</mapper>